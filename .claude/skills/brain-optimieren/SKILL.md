---
name: brain-optimieren
description: Vault-Gesundheitscheck, läuft brain-check.py (kaputte Links, isolierte Notizen, fehlendes Frontmatter, MOC-Lücken, große Notizen, Naming) und triagiert den Report. Auslösen bei "Brain optimieren", "Vault-Check", "Brain aufräumen", oder wenn der Vault auf Herz und Nieren geprüft/aufgeräumt werden soll. Zusätzlicher Tiefen-Modus bei "großer Brain-Review", "ist mein Brain noch effektiv".
---

# Brain optimieren / Vault-Gesundheitscheck

Die eigentliche Anleitung steht **im Vault** (Single Source of Truth).

**Lies die Datei und folge ihr Schritt für Schritt:**

```
.agents/skills/brain-optimieren/SKILL.md
```

Kurzfassung: erst Modus abfragen (Standard oder Standard + Tiefen-Modus) →
`Skripte/brain-check.py` laufen lassen → Report triagieren → bilanzieren.

Inhaltliche Änderungen am Vorgehen immer dort vornehmen, nicht hier.
